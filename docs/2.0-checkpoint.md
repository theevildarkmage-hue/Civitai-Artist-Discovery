# 2.0 resume checkpoint

Read this first, then git status --short and git log -5 --oneline.
Read only task-relevant source; do not repeat completed checks.

## Current state — 2026-09-06

- User requested completion and launch for testing. Local acceptance build is implemented
  and verified; the full release plan is NOT complete. See [2.0-testing.md](2.0-testing.md).
- Branch v2; sibling Civitai Desktop Client-v1 remains the main maintenance worktree.
  No push, publication, merge to main, or version bump. Internal package still says 1.0.4.
- Source server is still live at http://127.0.0.1:26997 (PID 357888) and its capture worker
  reports running. Static UI changes are live on refresh, but this pre-change process returns
  404 for `/api/history/calendar`, so saved/partial markers require an idle-time restart.
  Never interrupt the active collection merely to refresh the preview.

## Delivered

- Shared shell/navigation/headings/Settings, keyboard and native-dialog behavior.
- Image-first Gallery/Time Machine cards, adaptive safe previews, details, reactions,
  Follow. Profile uses the same Follow transaction; presentation moved to ui/profile.js.
- Combined filters, chips/count/reset, validated state/model-query serialization and deep
  restore. Removed superseded filter handlers. Advanced controls remain in preferences.
- Initial24 cards, shared paging promise, cancellation, manual retry guard, skeletons,
  empty/end messages. Time Machine shares skeletons; both secondary pages have retry UI.
- Inline cached tag decisions, one-connection tag reads, safe hidden-preview verification.
- New archive_tags_name_image index and targeted hidden-tag query (additive/v1-compatible).
- Local opt-in diagnostics, read-only measurement scripts, testing/rollback docs.
- Unified sticky command bar across Gallery, Time Machine, and Profile. Gallery date/window,
  feed, filters, and preferences now live in the shared navigation row.
- Calendar date picker with saved/partial coverage markers, disabled future dates, explicit
  All day/Morning/Evening choices, Latest saved, and Rebuild selected.
- Compact generation-model filters with search and an eight-item default; card size and
  discovery thresholds are presented as sliders and boolean preferences as switches.

## Verification

- Integration: **74/74 passed**, including new calendar backend/UI coverage and updated
  navigation compatibility checks.
- Eighteen current synthetic screenshots: reports/v2/current/, no overflow. Desktop Gallery
  and phone Calendar reviewed. User visual/zoom/manual tray checks remain.
- Final package: reports/v2/calendar-review-package/dist/CivitaiArtistDiscovery.
  Build and updater smoke passed: data/unrelated-file preservation, server restart,
  23 shared assets, nine offline browser views, no JS errors/overflow.
- Offline: warm187ms, feed25–41ms, model110ms, content76ms, restore74 cards.
  Cold skeleton256ms misses provisional200ms; specific follow-up is in testing notes.
- Real pre-index browser during collection/tests: fresh7499ms, warm4225ms. Not a controlled
  benchmark. Day/artist endpoints dominate; no claim whole-app targets are met.
- Disposable real-DB snapshot: hidden lookup611.76ms ->55.94ms, identical decisions for
  68,515 images across3,869,527 tag rows. Benchmark did not mutate the source DB.

## Next actions

1. Check capture status. If idle, restart the source app so the calendar coverage endpoint
   is available, then provide its URL for user acceptance.
2. Verify index initialization and repeat scripts/measure_preview.py after background work
   settles. Complete before claiming real-data performance targets.
3. Triage user acceptance feedback. Existing collection/auth orchestration and base CSS
   are deliberate retained integration code; further cleanup is incremental.
4. Remaining gates: manual tray/zoom/usability review, real-data rollback with v1, beta
   version/publish approval, beta feedback, stable merge/tag/release. M8 remains pending.

## Useful commands

    py tests/v2_shared_ui.py
    py tests/preview_tag_batch.py
    py scripts/measure_preview.py http://127.0.0.1:PORT --date YYYY-MM-DD
    ./scripts/build_exe.ps1 -OutputRoot reports/v2/final-review-package
    py scripts/validate_packaged_updater.py reports/v2/final-review-package/dist/CivitaiArtistDiscovery --browser
