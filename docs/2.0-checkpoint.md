# 2.0 resume checkpoint

Read this file first. Then inspect `git status --short` and `git log -5 --oneline`.
Read only the task-relevant source and milestone section, not the complete app or plan.

## Authorization and branches

- User approved the plan on 2026-09-05 and asked to keep working while away, documenting
  blockers and moving to independent work. Do not ask for plan approval again.
- Current workspace: `v2`. Baseline commit: `5fcf37d`.
- V1 checkout: sibling `Civitai Desktop Client-v1`, on `main` at `41fcea7`.
  Implement v1 hotfixes there, test, then merge `main` into `v2` and test the affected path.
- No account data was copied to the v1 checkout. All tests use temporary or synthetic data.
- No releases, remote pushes, or application version bump have been performed.

## Delivered

- Offline screenshots and browser resource timings for all three pages at 390/1366/1920.
- Native module bootstrap with a temporary classic-script controller bridge.
- Shared header/navigation, icons, design tokens, focus/skip link, popover Escape/Down keys,
  and native-dialog naming, close buttons, backdrop clicks, and focus-return contract.
- Common API and artwork modules, including preservation of successful original fallback
  when an unchanged card repaints.
- Shared gallery skeletons and immediate cancellation of stale artist-page requests.
- Initial page of 24, shared in-flight paging promise, and verified 74-card refresh restore.
- Card-width/density thumbnail buckets with visible-image priority; detail URLs unchanged.
- Content-level changes preserve the model filter.
- Shared card controller extracted to `static/ui/cards.js`; Gallery and Time Machine now
  use the same image-first layout. Latest full suite after these changes: 73/73 passed.
- Combined Filters panel, active count, removable model chips, and safe-default reset.
- Shared Profile/Time Machine page styling; Profile settings moved below insights.
- Useful empty/end-of-feed messages and an in-place retry for failed page requests.
- Batched cached tag reads; known tag decisions delivered in Gallery/Time Machine cards.
  Unknown previews still use viewport verification; hidden previews never request artwork.

## Verification

- Before changes: all 69 existing tests passed.
- Focused shared UI test verifies keyboard behavior, API errors/abort/headers, artwork
  fallback, cached/unknown/hidden previews, and view replacement while a stale response
  is held. Passes.
- Cached-tag microbenchmark: median 70.22 ms per-image versus 1.51 ms batched for 50
  synthetic images (five runs). This is not a real CDN or end-to-end speed claim.
- Current screenshot fixture: nine views, no horizontal overflow (baseline Gallery
  overflowed at 390 px). Review `reports/v2/shared-shell/` locally.
- Post-change full suite: 72/72 passed. Final focused checks also passed for shared UI,
  card size/content preferences, detail scrolling at five sizes, and updater dialogs.
- Cards milestone full suite: 73/73 passed. Latest filters/pages full run: 72/73, with an
  automatic-retry race found in shared UI. Fixed the guard and reran shared UI twice plus
  headless gallery, loading error recovery, and loading scenarios; all passed. Run the
  complete suite again for final integration/release validation.

## Next work

1. Review the working three-page UI in `reports/v2/unified-pages/` (includes filter panels).
   Continue M4–M6 polish: display preferences placement, profile creator actions, keyboard
   and narrow-window behavior. Do not repeat the shared-card/filter extraction.
2. Consolidate legacy style overrides and controller adapters incrementally (M7). The
   card controller is shared; some profile actions and filter state remain in app.js.
3. Finish M2 with real-network measurement and initial startup feedback. Offline cold/warm,
   feed/model/content and deep-restore scenarios now exist in `tests/v2_loading_scenarios.py`.
   Cold initial skeleton latency still exceeds the provisional 200 ms target on some runs.

Next files: `static/ui/`, `static/app.js` (targeted functions), `tests/v2_shared_ui.py`,
`tests/v2_visual_baseline.py`, and the relevant milestone in `docs/2.0-project-plan.md`.

## Open items

- M1 shared foundation is implemented and tested; existing page internals intentionally
  remain mounted in the shared shell. M0's broader timing scenarios and M2's remaining
  measurements/optimizations are open. Menus enhance existing panels; page controllers
  and dialog content still live in legacy markup/JavaScript pending final consolidation.
- User visual review pending; proceed with independent work under existing authorization.
- Packaged nested-asset/Windows smoke test and real-data performance not yet run.
- Synthetic screenshots use generated geometric artwork, not account content.
- Latest committed implementation before this checkpoint: `2f61719` (shared cards),
  preceded by `97108aa` (paging/previews/restore). Consult git log for the filters/pages commit.
